{{> reverse-proxy-core.conf}}

# Dashboard-length timeouts so a slow Stripe/PayPal call during signup
# cannot 504 while the charge still completes.
proxy_connect_timeout 3s;
proxy_send_timeout 3m;
proxy_read_timeout 3m;
