Dropbox sync issue

The daily sync validation check detected previously unsynced changes from Dropbox for the following sites:

{{#blogs}}
- {{id}}{{#handle}} ({{handle}}){{/handle}} — {{changeCount}} change{{#changeCountPlural}}s{{/changeCountPlural}}
  `logs blue | grep "{{truncatedId}} sync_"`
{{/blogs}}