iCloud sync issue

The hourly sync validation detected previously unsynced changes from iCloud for the following sites:

{{#blogs}}
- {{id}} {{handle}} — {{changeCount}} change{{#changeCountPlural}}s{{/changeCountPlural}}
  (downloaded: {{downloaded}}, removed: {{removed}}, created dirs: {{createdDirs}})
  `logs blue | grep "{{truncatedId}} sync_"`
  `access {{handle}}`
{{/blogs}}
